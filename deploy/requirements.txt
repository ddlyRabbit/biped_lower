# Deploy dependencies for biped_lower
#
# RPi 5 (Ubuntu 24.04, Python 3.12, ROS2 Jazzy):
#   pip3 install --break-system-packages -r requirements.txt
#
# Jetson Orin Nano (Ubuntu 22.04, Python 3.10, ROS2 Humble):
#   pip3 install -r requirements.txt

numpy>=1.21
scipy>=1.7
onnxruntime>=1.17        # ONNX policy inference
pyyaml>=5.4              # config file loading
python-can>=4.0          # CAN bus driver (socketcan backend)

# Optional — only needed for ZMP trajectory generation (pinocchio IK)
# pin>=3.9.0             # builds from source on arm64 (~5 min)

# Optional — BNO085 IMU (RPi 5 only, I2C)
# adafruit-circuitpython-bno08x
# adafruit-blinka
# sudo apt install python3-lgpio  (for GPIO reset)
